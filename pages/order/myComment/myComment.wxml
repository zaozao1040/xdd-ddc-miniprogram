<view wx:if='{{already}}'>
    <view class='top'>
        <image src='{{headImage}}' class='img'></image>
        <view class='name'>{{userName}}</view>
        <view class='date'>{{detail.evaluateTime}}</view>
    </view>
    <c-comment detail='{{detail}}'></c-comment>
</view>