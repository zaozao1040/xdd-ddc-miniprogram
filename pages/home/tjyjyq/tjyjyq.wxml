<view class="m-qmn">
  <van-cell-group>
    <van-field value="{{ username }}" label="真实姓名" placeholder="请输入真实姓名" />
    <van-field value="{{ phone }}" label="手机号" placeholder="请输入手机号" />
    <van-field value="{{ phone }}" label="公司名称" placeholder="请输入公司名称" />
    <van-field value="{{ phone }}" label="公司地址" placeholder="请输入公司地址">
      <van-button slot="button" size="small" type="primary">选择区域</van-button>
    </van-field>
    <van-field value="{{ phone }}" label="详细地址" placeholder="请输入详细地址" />
    <van-field value="{{ phone }}" label="所属行业" placeholder="所属行业" />
    <van-field value="{{ phone }}" label="人员规模" placeholder="人员规模" />
  </van-cell-group>
  <block wx:if="{{showArea}}">
    <van-area area-list="{{ areaList }}" />
  </block>
</view>